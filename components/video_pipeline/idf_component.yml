dependencies:
  # Hardware H.264 encoder. Availability is probed at boot rather than assumed:
  # the encoder is tied to the chip revision, and this board carries a v1.3.
  espressif/esp_h264:
    version: "^1.3.0"
