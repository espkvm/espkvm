# ESP-KVM partition table.
#
# Two app slots, because a KVM is often the only way to reach the machine it is
# attached to: an update that bricks it may also remove the means of fixing it.
# With two slots the bootloader falls back to the previous image.
#
# The board carries 32 MB of flash. 4 MB per slot leaves room for a firmware
# that grows an H.264 encoder, TLS and a USB mass storage stack without another
# partition change, and the remainder becomes a storage area - disk images for
# virtual media live on the microSD card, but certificates, logs and a custom
# EDID need somewhere on-device.
#
# The 4 MB "rescue" partition holds a small bootable disk image (iPXE, memtest,
# a DOS floppy) served to the target over USB straight from flash - no card
# needed, and flash writes are reliable here where the microSD's are not, so it
# can be re-flashed from the console. It is kept deliberately small: a full OS
# image does not fit, and is not the point - that is what the microSD is for.
# Adding this partition leaves every offset above unchanged, so a device adopts
# it with a one-time cable flash of the table and keeps its app slots.
#
#
# The 56 KB between phy_init and the first app slot is free space every ESP-IDF
# layout leaves behind: app slots start on a 64 KB boundary, and the data
# partitions above end at 0x12000. A core dump lives there, so adding it moves
# no offset at all - a device adopts it by flashing the table alone and keeps
# both app slots, its storage and its rescue image. Without the partition the
# panic handler says so and reboots as before; nothing is written.
#
# Name       Type  SubType   Offset      Size
nvs,         data, nvs,      0x9000,     0x6000
otadata,     data, ota,      0xf000,     0x2000
phy_init,    data, phy,      0x11000,    0x1000
coredump,    data, coredump, 0x12000,    0xe000
ota_0,       app,  ota_0,    0x20000,    0x400000
ota_1,       app,  ota_1,    0x420000,   0x400000
storage,     data, spiffs,   0x820000,   0x400000
rescue,      data, 0x40,     0xc20000,   0x400000
